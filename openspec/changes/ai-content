## MODIFIED Requirements

### Requirement: AI Summary Generation with Style Selection
前端「🤖 AI 生成」按钮点击后 SHALL 弹出模式选择面板，用户选择一种风格后调用后端生成简介。后端 `POST /ai/summary` SHALL 接受 `style` 参数。

#### Scenario: 弹出模式选择面板
- **WHEN** 用户在编辑器中点击「🤖 AI 生成」按钮
- **AND** 正文内容非空
- **THEN** 弹出一个模态面板，显示 6 种模式选项

#### Scenario: 可选择 6 种模式
- **WHEN** 模式选择面板显示
- **THEN** 面板包含以下选项：
  - 「📋 正式公告」— 严谨正式的公告式简介
  - 「📢 营销吸睛」— 夸张吸引眼球的营销风格
  - 「🎓 学术简洁」— 学术论文摘要风格
  - 「💬 轻松娱乐」— 轻松口语化风格
  - 「🤪 幽默搞笑」— 幽默玩梗风格
  - 「✏️ 自定义」— 用户输入自定义提示词

#### Scenario: 选择模式后生成
- **WHEN** 用户在面板中选择一种模式（非自定义）
- **THEN** 面板关闭，调用 `POST /ai/summary` 传入 `style` 参数
- **AND** 显示 loading 状态
- **AND** 生成成功后填入简介框并显示模型来源标注

#### Scenario: 自定义模式
- **WHEN** 用户选择「✏️ 自定义」
- **THEN** 面板中显示一个文本输入框
- **WHEN** 用户填写自定义提示词并提交
- **THEN** 调用 `POST /ai/summary` 传入 `style="custom"` 和 `custom_prompt`

#### Scenario: 取消选择
- **WHEN** 模式选择面板显示时用户点击关闭或取消
- **THEN** 面板关闭，不调用 API

### Requirement: Backend Summary Style Support
后端 `POST /ai/summary` SHALL 支持 `style` 可选参数，默认值为 `"formal"`。

#### Scenario: style 参数映射
- **WHEN** 接收到 `style="formal"` → 使用正式公告 prompt
- **WHEN** 接收到 `style="marketing"` → 使用营销吸睛 prompt
- **WHEN** 接收到 `style="academic"` → 使用学术简洁 prompt
- **WHEN** 接收到 `style="casual"` → 使用轻松娱乐 prompt
- **WHEN** 接收到 `style="humorous"` → 使用幽默搞笑 prompt
- **WHEN** 接收到 `style="custom"` → 使用 `custom_prompt` 作为 system prompt

#### Scenario: 默认 style
- **WHEN** 请求体中未传 `style` 字段
- **THEN** 默认使用 `"formal"` 模式