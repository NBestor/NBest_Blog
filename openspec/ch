## ADDED Requirements

### Requirement: Backend Logging Configuration
后端 SHALL 使用 Python `logging` 模块配置统一日志系统，输出到文件和控制台。

#### Scenario: 日志配置文件输出
- **WHEN** 后端启动时调用 `setupLogging()`
- **THEN** 日志文件写入 `logs/app.log`
- **AND** 日志格式包含时间戳、日志级别、模块名、消息内容
- **AND** 文件大小超过 5MB 时自动轮转，保留最近 3 个文件

#### Scenario: 控制台日志级别过滤
- **WHEN** 后端运行中发生 WARNING 或 ERROR 级别事件
- **THEN** 日志同时输出到控制台
- **AND** INFO 和 DEBUG 级别日志仅写入文件，不输出到控制台

### Requirement: API Route Error Logging
关键 API 路由 SHALL 在捕获异常时记录错误日志。

#### Scenario: API 异常记录
- **WHEN** `articles.py`, `ai.py`, `users.py`, `quick_posts.py`, `comments.py` 中的路由捕获异常
- **THEN** 使用模块级 logger 记录错误信息
- **AND** 日志包含请求方法、路径、用户 ID（如已登录）、异常详情

### Requirement: AI Service Call Logging
AI 服务层 SHALL 记录每次 API 调用的详细信息。

#### Scenario: AI 调用成功日志
- **WHEN** `_callAI()` 成功返回结果
- **THEN** 记录调用开始（model, contentLen, maxTokens）
- **AND** 记录调用成功（elapsed 秒数, responseLen）

#### Scenario: AI 调用失败日志
- **WHEN** `_callAI()` 抛出异常
- **THEN** 记录调用失败（elapsed 秒数, 异常信息）
- **AND** 重新抛出异常（不吞掉错误）