## Why

当前牛宝评论功能需要用户手动触发（在文章编辑页或主页点击"🤖 牛宝评论"按钮），生成单条评论后即结束。用户无法与牛宝进行多轮对话式互动——当有人回复牛宝的评论时，牛宝没有任何回应，显得不够"智能"和"温暖"。

需要让牛宝在被回复时自动再回复，形成真正的对话互动体验，增加趣味性和用户粘性。

## What Changes

- **后端**：在评论创建流程中（`createTargetComment`），检测新评论的父评论作者是否为牛宝（user_id=666）。如果是，异步调用 AI 生成一条上下文感知的回复评论。
- **后端**：新增 `NIUBAO_REPLY_PROMPT` — 用于多轮对话式回复的 system prompt，让牛宝根据对话历史上下文生成自然、连贯的回复。
- **后端**：新增 `generateReply()` 函数 — 接收对话上下文（原内容 + 被回复的评论 + 回复内容），生成牛宝的回复。
- **防无限循环**：牛宝不会回复自己的自动回复（即不回复 author=666 的评论），也不会在 AI 调用失败时阻塞用户评论的正常创建流程。

## Capabilities

### New Capabilities

- `niubao-auto-reply`: 牛宝被回复时自动回复，形成对话互动闭环。

## Impact

| 层级 | 文件 | 改动 |
|------|------|------|
| 后端 service | `ai_service.py` | 新增 `NIUBAO_REPLY_PROMPT` 和 `generateReply()` 函数 |
| 后端 service | `comment_service.py` | 在 `createTargetComment()` 中检测父评论是牛宝时触发自动回复 |
| 后端 API | 无需改动 | — |
| 前端 | 无需改动 | 自动回复在前端正常显示，无感知 |

- 不影响现有评论创建流程（用户正常回复评论的逻辑不变）
- 不阻塞用户操作（AI 调用以异步方式执行，失败时静默降级）
- 不开辟新的 HTTP 路由或前端组件