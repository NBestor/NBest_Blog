## ADDED Requirements

### Requirement: AI Polish Article Content
编辑器 SHALL 新增「🤖 AI 润色」按钮，点击后弹出模式选择面板，用户选择润色模式后调用 `POST /ai/polish`，返回润色后的内容替换编辑器正文。

#### Scenario: 润色按钮存在
- **WHEN** 用户在文章编辑器中编辑正文
- **THEN** 编辑器操作区显示「🤖 AI 润色」按钮
- **AND** 正文为空时按钮不可用

#### Scenario: 弹出润色模式选择面板
- **WHEN** 用户点击「🤖 AI 润色」按钮
- **AND** 正文非空
- **THEN** 弹出模态面板，显示 5 种润色模式选项

#### Scenario: 5 种润色模式
- **WHEN** 润色面板显示
- **THEN** 面板包含以下选项：
  - 「📐 仅优化排版」— 只调整段落空行、标题层级，**严禁修改任何一个字**
  - 「🔍 仅改错别字/重复」— 只修正错字和重复内容，不改变原意
  - 「🎓 学术规范」— 学术化改写，专业严谨
  - 「🌸 青春文学」— 青春文学风格，文艺清新
  - 「✏️ 自定义」— 用户输入自定义提示词

#### Scenario: 润色后替换正文
- **WHEN** 用户选择模式并润色成功
- **THEN** 面板关闭
- **AND** 编辑器正文被替换为润色后的内容

#### Scenario: 自定义模式
- **WHEN** 用户选择「✏️ 自定义」
- **THEN** 面板中显示文本输入框
- **WHEN** 用户填写自定义提示词并提交
- **THEN** 调用 `POST /ai/polish` 传入 `style="custom"` 和 `custom_prompt`

### Requirement: Backend Polish API
后端 SHALL 新增 `POST /ai/polish` 路由。

#### Scenario: 润色请求
- **WHEN** 接收到 `POST /ai/polish` 请求体 `{ content, style, custom_prompt? }`
- **THEN** 根据 style 选择对应 system prompt 调用 AI
- **AND** 返回 `{ polished, model }`

#### Scenario: style 映射
- `formatting` → 排版优化 prompt（严禁修改文字）
- `typo` → 错别字修正 prompt
- `academic` → 学术规范 prompt
- `youth_lit` → 青春文学 prompt
- `custom` → 使用 `custom_prompt`